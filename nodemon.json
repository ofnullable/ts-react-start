{
  "watch": [
    "src/server.ts",
    "src/middleware/**",
    "nodemon.json"
  ],
  "exec": "NODE_ENV=development npm run build && NODE_ENV=development node ./dist/server",
  "ext": "tsx ts js json"
}
